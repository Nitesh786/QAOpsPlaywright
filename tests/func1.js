function add (a,b) // we need to give parameter here as a,b
{
   return a+b  // added return keyword here because we need sum of a+b in return
}
var result = add(4,6)  // calling function add here & giving parameter as 4,6
console.log(result)